import {NextRequest,NextResponse}from'next/server';
export function proxy(request:NextRequest){const path=request.nextUrl.pathname;if(path.startsWith('/api/v1/coaching'))return NextResponse.next();const localAuthEnabled=process.env.AUTH_MODE!=='oidc'&&process.env.BOOTSTRAP_ACKNOWLEDGEMENT==='create-initial-admin';if(localAuthEnabled&&(path==='/api/auth/login'||path==='/api/auth/me'))return NextResponse.next();const legacyApi=path.startsWith('/api/');const generated=path.startsWith('/batch09/')||path.startsWith('/codex/')||path==='/admin';const oldDashboard=path.startsWith('/dashboard/')&&path!=='/dashboard';if((legacyApi||generated)&&!(process.env.NODE_ENV!=='production'&&process.env.ENABLE_LEGACY_DEMOS==='1'))return NextResponse.json({error:'Legacy demo, generated-gap, generic-AI, and unscoped admin routes are quarantined'},{status:404});if(oldDashboard)return NextResponse.redirect(new URL('/dashboard',request.url));return NextResponse.next();}
export const config={matcher:['/api/:path*','/batch09/:path*','/codex/:path*','/dashboard/:path*','/admin']};
